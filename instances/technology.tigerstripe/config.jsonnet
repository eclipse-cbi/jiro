local default = import '../../templates/config.libsonnet';

default+ {
  project+: {
    fullName: "technology.tigerstripe",
    shortName: "tigerstripe",
    displayName: "Eclipse Tigerstripe"
  }
}
